import 'package:flutter/material.dart';

/// Defines the color palette used across the application.
/// 
/// This class centralizes all design colors to ensure visual
/// consistency and maintainability throughout the project.
class AppColors {

  /// Main application background color.
  static const Color background = Color(0xFF202020);

  /// Surface color for cards, containers and elevated elements.
  static const Color surface = Color(0xFF3B3B3B);

  /// Primary brand color.
  static const Color primaryGold = Color(0xFFD4AF37);

  /// Darker variation of the primary brand color.
  static const Color darkGold = Color(0xFFB8902E);

  /// Primary text color.
  static const Color textPrimary = Colors.white;

  /// Secondary text color for less prominent content.
  static const Color textSecondary = Color(0xFFB3B3B3);
}